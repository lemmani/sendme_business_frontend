
import 'package:flutter/material.dart';

class CustomIconsRes {
  static const Icon logo = Icon(Icons.local_shipping, size: 54, color: Color(0xFF0F52BA));
  static const Icon wifi = Icon(Icons.wifi, size: 20, color: Color(0xFF3A353A));
  static const Icon cellular = Icon(Icons.signal_cellular_alt, size: 16, color: Color(0xFF3A353A));
  static const Icon battery = Icon(Icons.battery_full, size: 25, color: Color(0xFF3A353A));
  static const Icon progressIcon = Icon(Icons.check_circle, size: 24, color: Color(0xFF0F52BA));
  static const Icon locationPin = Icon(Icons.location_on, size: 20, color: Color(0xFF3A353A));
}